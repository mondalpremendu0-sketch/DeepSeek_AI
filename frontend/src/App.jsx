import { router } from "./app.routes.js";
import ChatProvider from "./app.routes.js";

function App() {
    return;
    <ChatProvider>
        <RouterProvider router={router} />;
    </ChatProvider>;
}

export default App;
